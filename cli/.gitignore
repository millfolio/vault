# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Build artifacts / bundles
*.app
*.dmg
*.pkg
# Millfolio.icns is a tracked asset (generated by installer/make_icon.swift); the
# intermediate .iconset is not.
installer/Millfolio.iconset/

# Xcode (if a project is added later)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
